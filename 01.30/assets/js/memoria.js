const cartas = [1, 1, 2, 2, 3, 3, 4, 4];

async function geraImagemPar(){
    const imagemPar = {};
    for (let i = 0; i < cartas.length; i++) {
        if (!imagemPar[cartas[]]) {
            const id = 
        }
    }
}